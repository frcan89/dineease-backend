type: object
required:
  - passwordActual
  - nuevaPassword
properties:
  passwordActual:
    type: string
    format: password
  nuevaPassword:
    type: string
    format: password
    minLength: 6 # Ejemplo de validación